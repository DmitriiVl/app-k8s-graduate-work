name: Docker-Image-App-K8S

on:
  push:
    branches: "main"

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
      run: docker build -t milvo/app-k8s-graduate-work:$(date +%s) .
